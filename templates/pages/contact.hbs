<section class="py-20">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="text-4xl md:text-5xl font-heading font-bold text-gray-900 text-center mb-4">Contact Us</h1>
    <p class="text-gray-500 text-center max-w-2xl mx-auto mb-14">{{ctaSubtext}}</p>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Contact info + hours -->
      <div class="space-y-8">
        {{> contact-info}}
        {{> hours}}
      </div>

      <!-- Map -->
      <div>
        {{> map-embed}}
      </div>
    </div>
  </div>
</section>

{{> cta-banner}}
